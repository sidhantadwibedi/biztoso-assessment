import { Routes } from '@angular/router';
import { LeadListComponent } from './components/lead-list/lead-list.component';

export const LEAD_ROUTES: Routes = [
  { path: '', component: LeadListComponent }, // /lead-generation
];
